const { SlashCommandBuikder } = require('discord.js')

module.exports = {
  data: new SlashCommandBuilder()
        .setName("okmec")
        .setDescription("tvq mec"),

  run: async ({ interaction }) => {
    
  }
}
